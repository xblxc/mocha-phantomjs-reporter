{
  "name": "mocha-phantomjs-reporter",
  "version": "0.0.3",
  "description": "a mocha reporter which we can use in phantomjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mocha",
    "phantomjs"
  ],
  "author": "xblxc",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "colour": "^0.7.1",
    "mocha": "^2.5.3"
  }
}
